"use strict";

// Graphics Settings

// UI
let renderUI = true; // Render ALL UI
let renderMiniMap = true; // Render the minimap if the UI is being rendered.

// EFFECTS
let renderLighting = true;

// Engine Settings
let paused = false;
